# 🧪 测试

## 快速开始

```bash
pnpm test          # 运行所有测试
pnpm test:unit     # 单元测试
pnpm test:web      # Web 测试
pnpm test:e2e      # E2E 测试
```

## 测试类型

### 单元测试 (`__tests__/`)

- 框架：Jest
- 环境：Node.js
- 测试：模块加载、文件提取、密码解压

### Web 测试 (`__webtests__/`)

- 框架：Jest + JSDOM
- 环境：模拟浏览器
- 测试：浏览器兼容性、WASM 加载

### E2E 测试 (`__e2etests__/`)

- 框架：Playwright
- 浏览器：Chromium, Firefox, WebKit
- 测试：真实浏览器场景、UI 交互

## 调试测试

```bash
# E2E 调试模式
pnpm test:e2e --debug

# E2E UI 模式
pnpm test:e2e --ui
```

## 测试文件

- `noencryption.rar` - 无密码测试文件
- `encryption.rar` - 加密测试文件（密码：`123`）
